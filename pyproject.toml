[project]
name = "whisper-dictate"
version = "0.1.0"
description = "A privacy-first, local speech-to-text and AI cleanup tool for Windows using faster-whisper and optional LLM cleanup"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "faster-whisper>=1.2.1",
    "keyring>=25.7.0",
    # Metaphone, for opt-in phonetic glossary rules.
    "jellyfish>=1.1.0",
    "numpy>=2.3.4",
    # CUDA 12 cuBLAS for ctranslate2 and llama-cpp, cudart for llama-cpp. Both link
    # the *64_12 names, so these stay on 12 even though torch is on 13. cuDNN
    # comes inside the ctranslate2 wheel; nothing loads nvrtc.
    "nvidia-cublas-cu12==12.4.5.8",
    "nvidia-cuda-runtime-cu12==12.4.127",
    "openai>=3.16",
    "sounddevice>=0.5.6",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.12.0",
    "ruff>=0.1.0",
    "mypy>=1.8.0",
]
# Heavy ML wheels: installed locally, excluded in CI (`uv sync --no-group ml`).
# The tests mock these; this Windows machine exercises the real thing.
ml = [
    "torch",
    "transformers>=5.4.0",
    "huggingface-hub",
    # CohereAsrFeatureExtractor imports librosa at class load, whatever the audio
    # source. Not optional, despite audio.py already yielding 16 kHz float32.
    "librosa",
    "llama-cpp-python>=0.3.35",
]

[project.scripts]
dictate-gui = "whisper_dictate.gui:main"

[build-system]
requires = ["hatchling>=1.21.0"]
build-backend = "hatchling.build"

[tool.uv]
package = true
default-groups = ["dev", "ml"]

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[[tool.uv.index]]
name = "llama-cu124"
url = "https://abetlen.github.io/llama-cpp-python/whl/cu124"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cu130", marker = "sys_platform == 'win32'" }]
llama-cpp-python = [{ index = "llama-cu124", marker = "sys_platform == 'win32'" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--cov=whisper_dictate",
    "--cov-report=term-missing",
    "--cov-report=html",
]

[tool.ruff]
line-length = 100
target-version = "py313"
# Ruff >= 0.16 also formats Python blocks inside Markdown; docs and plans are prose.
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]  # Line too long handled by formatter

[tool.ruff.lint.per-file-ignores]
# The clipboard fake mirrors Win32 entry points; their names are not ours to choose.
"tests/test_clipboard.py" = ["N802"]

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true
